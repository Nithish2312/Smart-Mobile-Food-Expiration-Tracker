import 'package:flutter/material.dart';

class PlaceholderWidget extends StatelessWidget {
  final Container container;

  PlaceholderWidget(this.container);

  @override
  Widget build(BuildContext context) {
    return container;
  }
}
